# CrossRef Local Configuration
# Copy this file to .env and customize as needed

# =============================================================================
# DATABASE MODE - Direct database access (default)
# =============================================================================

# Path to CrossRef SQLite database (optional - auto-detected if not set)
# CROSSREF_LOCAL_DB=/path/to/crossref.db

# =============================================================================
# HTTP MODE - Connect to HTTP API server
# =============================================================================

# Force mode: 'db', 'http', or 'auto' (default: auto)
# CROSSREF_LOCAL_MODE=http

# API URL for http mode (default: auto-detect from localhost ports)
# CROSSREF_LOCAL_API_URL=http://localhost:8333

# =============================================================================
# SERVER CONFIGURATION
# =============================================================================

# HTTP API server settings
# CROSSREF_LOCAL_HOST=0.0.0.0
# CROSSREF_LOCAL_PORT=8333

# MCP server settings
# CROSSREF_LOCAL_MCP_HOST=localhost
# CROSSREF_LOCAL_MCP_PORT=8082

# =============================================================================
# TYPICAL USAGE
# =============================================================================
#
# Local database (default):
#   crossref-local search "machine learning"
#
# HTTP mode (connect to running server):
#   crossref-local --http search "machine learning"
#   # Or set: export CROSSREF_LOCAL_MODE=http
#
# SSH tunnel to remote server:
#   ssh -L 8333:127.0.0.1:8333 your-server
#   crossref-local --http search "query"
#
# Start servers:
#   crossref-local run-server-http          # HTTP API on :8333
#   crossref-local run-server-mcp           # MCP server (stdio)
#   crossref-local run-server-mcp -t http   # MCP server (HTTP)
